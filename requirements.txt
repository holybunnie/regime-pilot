# Regime Pilot — pinned dependencies for reproducible, deterministic runs.
# Python 3.12.1 (see STATUS.md for the exact interpreter used).
jsonschema==4.26.0
eth-account==0.13.7
web3==7.16.0
requests==2.32.3
pandas==2.2.3
numpy==2.1.3
pyarrow==18.1.0
py-solc-x==2.0.3
matplotlib==3.11.0
# Test-only: in-memory EVM for the attestation unit test + reveal dry-run (Items 3, 6).
eth-tester[py-evm]==0.12.0b2
pytest==8.3.4
